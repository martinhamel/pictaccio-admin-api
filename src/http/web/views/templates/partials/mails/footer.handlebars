<table width="100%"
       cellspacing="0"
       cellpadding="0"
       style="border-collapse: collapse; width: 100%">
    <tr>
        <td>{{#if footerText }} {{ footerText }} {{/if}}</td>
    </tr>
    <tr>
        <td>{{#if storeLocation }} {{ storeLocation }} {{/if}}</td>
    </tr>
    <tr>
        <td>{{ reasonOrUnsubscribe }}</td>
    </tr>
    <tr>
        <td>{{t "mail:common.footer.poweredBy" }}</td>
    </tr>
</table>
